import 'package:flutter/material.dart';

class RecievedCapsuleDetailScreen extends StatelessWidget {
  const RecievedCapsuleDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("View"),
      ),
    );
  }
}
